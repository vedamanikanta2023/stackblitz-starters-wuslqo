import { makeStyles } from '@mui/styles';

const useStyles = makeStyles((theme) => ({
  logoutButton: {
    width: 30,
    position: 'absolute',
    right: '20px',
    top: '20px',
  },
}));

export default useStyles;
